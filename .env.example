# Connection string pooled dari Neon (Vercel)
DATABASE_URL=postgresql://postgres:password@host/db?sslmode=require

# Password untuk login panel admin (/admin)
ADMIN_PASSWORD=ganti-dengan-password-kuat

# Secret minimal 32 karakter untuk sesi admin
SESSION_SECRET=ganti-dengan-string-random-minimal-32-karakter
